import { Routes } from "@angular/router";
import { PostulationComponent } from "./postulation.component";


export const POSTULATION_ROUTES: Routes = [
  { path: '', component: PostulationComponent}
]